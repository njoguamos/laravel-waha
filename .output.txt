Command finished with exit code 0.
Command output:
diff --git a/AGENTS.md b/AGENTS.md
index 8c5074e..d51bdb2 100644
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -26,7 +26,7 @@ This is **Laravel WAHA** - a PHP Laravel package that provides an elegant API cl
 │   ├── Feature/      # Feature tests
 │   ├── Unit/         # Unit tests
 │   ├── Pest.php      # Pest configuration
-│   └── TestCase.php  # Base test case
+│   └── TestCase.php  # Base test case (not used for test files)
 ```
 
 ### Core Patterns
@@ -34,8 +34,9 @@ This is **Laravel WAHA** - a PHP Laravel package that provides an elegant API cl
 1. **Saloon HTTP:** `WahaConnector` (auth & headers) → `Request` classes (per endpoint) → DTOs
 2. **Endpoints:** Extend `Waha` base class, return typed DTOs, accessible via facades
 3. **DTOs:** Have `toArray()` serialization. `fromArray()` is **prohibited**.
-4. **Service Provider:** Binds `WahaConnector` to container, publishes config
-5. **Facades:** Static access via `Status::sendText()` instead of `app(Status::class)->sendText()`
+4. **Testing:** All tests MUST use **Pest PHP** syntax (`test()` or `it()`). PHPUnit class-based tests are prohibited.
+5. **Service Provider:** Binds `WahaConnector` to container, publishes config
+6. **Facades:** Static access via `Status::sendText()` instead of `app(Status::class)->sendText()`
 
 ## Coding Standards
 

diff --git a/docs/send-messages/send-text.md b/docs/send-messages/send-text.md
index c20b3c8..3d5216d 100644
--- a/docs/send-messages/send-text.md
+++ b/docs/send-messages/send-text.md
@@ -16,6 +16,8 @@ $response = Message::sendText(data: $data);
 
 ::: warning Send Typing Status
 Enabling `send_typing_status` in your `config/waha.php` reduces the chances of being blocked by WhatsApp but may add 3–30 seconds of blocking latency to every `sendText()` call.
+
+The package mimics human behavior by sending "online", "typing", and "paused" presence statuses. If any of these presence updates fail (e.g., due to engine limitations), the error is logged, and the message sending continues.
 :::
 
 ## Reply to message

diff --git a/src/Endpoints/Message.php b/src/Endpoints/Message.php
index 88d2662..106f8e4 100644
--- a/src/Endpoints/Message.php
+++ b/src/Endpoints/Message.php
@@ -9,6 +9,7 @@ use Saloon\Http\Response;
 use Random\RandomException;
 use NjoguAmos\Waha\Dto\SeenData;
 use NjoguAmos\Waha\Enums\Presence;
+use Illuminate\Support\Facades\Log;
 use NjoguAmos\Waha\Dto\PresenceData;
 use NjoguAmos\Waha\Dto\MessageTextData;
 use Saloon\Exceptions\Request\RequestException;
@@ -61,27 +62,51 @@ class Message extends Waha
     private function sendPresenceStatus(string $session, string $chatId): void
     {
         // 1. Send Online status
-        $this->connector->send(new SetPresenceRequest(
-            session: $session,
-            data: new PresenceData(presence: Presence::ONLINE)
-        ));
+        try {
+            $this->connector->send(new SetPresenceRequest(
+                session: $session,
+                data: new PresenceData(presence: Presence::ONLINE)
+            ));
+        } catch (FatalRequestException|RequestException $exception) {
+            Log::error(message: $exception->getMessage(), context: [
+                'session' => $session,
+                'chatId'  => $chatId,
+            ]);
+        }
 
-        sleep(random_int(min: 1, max: 10));
+        try {
+            sleep(random_int(min: 1, max: 10));
 
-        // 2. Send Typing status
-        $this->connector->send(new SetPresenceRequest(
-            session: $session,
-            data: new PresenceData(presence: Presence::TYPING, chatId: $chatId)
-        ));
+            // 2. Send Typing status
+            $this->connector->send(new SetPresenceRequest(
+                session: $session,
+                data: new PresenceData(presence: Presence::TYPING, chatId: $chatId)
+            ));
+        } catch (FatalRequestException|RequestException $exception) {
+            Log::error(message: $exception->getMessage(), context: [
+                'session' => $session,
+                'chatId'  => $chatId,
+            ]);
+        }
 
-        sleep(random_int(min: 1, max: 10));
+        try {
+            sleep(random_int(min: 1, max: 10));
 
-        // 3. Send Paused status
-        $this->connector->send(new SetPresenceRequest(
-            session: $session,
-            data: new PresenceData(presence: Presence::PAUSED, chatId: $chatId)
-        ));
+            // 3. Send Paused status
+            $this->connector->send(new SetPresenceRequest(
+                session: $session,
+                data: new PresenceData(presence: Presence::PAUSED, chatId: $chatId)
+            ));
+        } catch (FatalRequestException|RequestException $exception) {
+            Log::error(message: $exception->getMessage(), context: [
+                'session' => $session,
+                'chatId'  => $chatId,
+            ]);
+        }
 
-        sleep(random_int(min: 1, max: 10));
+        try {
+            sleep(random_int(min: 1, max: 5));
+        } catch (RandomException) {
+        }
     }
 }

diff --git a/src/Enums/SessionStatus.php b/src/Enums/SessionStatus.php
new file mode 100644
index 0000000..3592017
--- /dev/null
+++ b/src/Enums/SessionStatus.php
@@ -0,0 +1,33 @@
+<?php
+
+declare(strict_types=1);
+
+namespace NjoguAmos\Waha\Enums;
+
+/**
+ * Session status values based on WAHA API documentation.
+ *
+ * @see https://waha.devlike.pro/docs/how-to/sessions/#session-status
+ */
+enum SessionStatus: string
+{
+    case STOPPED = 'STOPPED';
+    case STARTING = 'STARTING';
+    case SCAN_QR_CODE = 'SCAN_QR_CODE';
+    case WORKING = 'WORKING';
+    case FAILED = 'FAILED';
+
+    /**
+     * Get the label for the session status.
+     */
+    public function label(): string
+    {
+        return match ($this) {
+            self::STOPPED      => 'Session is stopped',
+            self::STARTING     => 'Session is starting',
+            self::SCAN_QR_CODE => 'Session is required to scan QR code or login via phone number',
+            self::WORKING      => 'Session is working and ready to use',
+            self::FAILED       => 'Session is failed due to some error',
+        };
+    }
+}

diff --git a/tests/Feature/MessageTest.php b/tests/Feature/MessageTest.php
index de9e3df..c36f1a6 100644
--- a/tests/Feature/MessageTest.php
+++ b/tests/Feature/MessageTest.php
@@ -5,6 +5,7 @@ declare(strict_types=1);
 use NjoguAmos\Waha\Dto\SeenData;
 use NjoguAmos\Waha\Enums\Presence;
 use Saloon\Http\Faking\MockClient;
+use Illuminate\Support\Facades\Log;
 use NjoguAmos\Waha\Facades\Message;
 use Saloon\Http\Faking\MockResponse;
 use NjoguAmos\Waha\Dto\MessageTextData;
@@ -123,6 +124,37 @@ describe(description: 'send text message', tests: function () {
         // 4. Send Text
         expect($sentRequests[3]->getRequest())->toBeInstanceOf(SendTextRequest::class);
     });
+
+    it(description: 'continues sending text message even if presence status fails and logs the error', closure: function () {
+        config()->set(key: 'waha.send_typing_status', value: true);
+
+        Log::shouldReceive('error')
+            ->times(3)
+            ->withArgs(function ($message, $context) {
+                return str_contains($message, 'Internal Server Error')
+                    && $context === [
+                        'session' => 'default',
+                        'chatId'  => '123456789@c.us',
+                    ];
+            });
+
+        MockClient::global(mockData: [
+            SetPresenceRequest::class => MockResponse::make(body: ['error' => 'Internal Server Error'], status: 500),
+            SendTextRequest::class    => MockResponse::make(body: [], status: 201)
+        ]);
+
+        $data = new MessageTextData(
+            chatId: '123456789@c.us',
+            text: 'Still sent even if presence fails.'
+        );
+
+        $result = Message::sendText(data: $data);
+
+        expect(value: $result->status())->toBe(expected: 201);
+
+        // Verify that SendTextRequest was still sent
+        MockClient::global()->assertSent(SendTextRequest::class);
+    });
 });
 
 describe(description: 'send seen', tests: function () {

diff --git a/tests/Unit/Enums/SessionStatusTest.php b/tests/Unit/Enums/SessionStatusTest.php
new file mode 100644
index 0000000..954349c
--- /dev/null
+++ b/tests/Unit/Enums/SessionStatusTest.php
@@ -0,0 +1,27 @@
+<?php
+
+declare(strict_types=1);
+
+use NjoguAmos\Waha\Enums\SessionStatus;
+
+test(description: 'it has expected cases', closure: function () {
+    $expected = [
+        'STOPPED',
+        'STARTING',
+        'SCAN_QR_CODE',
+        'WORKING',
+        'FAILED',
+    ];
+
+    $actual = array_column(SessionStatus::cases(), 'value');
+
+    expect($actual)->toBe($expected);
+});
+
+test(description: 'it returns correct labels', closure: function () {
+    expect(SessionStatus::STOPPED->label())->toBe('Session is stopped')
+        ->and(SessionStatus::STARTING->label())->toBe('Session is starting')
+        ->and(SessionStatus::SCAN_QR_CODE->label())->toBe('Session is required to scan QR code or login via phone number')
+        ->and(SessionStatus::WORKING->label())->toBe('Session is working and ready to use')
+        ->and(SessionStatus::FAILED->label())->toBe('Session is failed due to some error');
+});