# Session Webhook HMAC Data DTO Reference

The `NjoguAmos\Waha\Dto\SessionWebhookHmacData` contains HMAC configuration for session webhooks.

## `key` → `string`

The HMAC secret key.

```php
$hmac->key; // "your-secret-key"
```
